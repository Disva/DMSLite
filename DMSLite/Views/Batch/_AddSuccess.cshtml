@model DMSLite.Entities.Batch

@{ var id = Guid.NewGuid().ToString(); }

<div id="@id">
    <div class="modal-header">
        <button type="button" class="close" aria-label=@Resources.Resources.UI_Close data-dismiss="modal"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="myModalLabel">@Resources.Resources.UI_AddBatchSuc</h4>
    </div>
    <div class="modal-body">
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Id)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Id)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Title)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Title)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.CreateDate)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.CreateDate)
            </dd>
        </dl>
    </div>
    @using (Ajax.BeginForm("AddForm", "Batch", new AjaxOptions()
    {
        InsertionMode = InsertionMode.Replace,
        UpdateTargetId = id
    }))
    {
        <div class="modal-footer">
            <div class="form-group">
                <div class="col-md-offset-2 col-md-10" id="createButton">
                    <button type="button" class="btn btn-default" data-dismiss="modal">@Resources.Resources.UI_Close</button>
                    <input type="submit" value=@Resources.Resources.UI_AddAnoBatch class="btn btn-primary" />
                </div>
            </div>
        </div>
    }
</div>